# Your Solution
apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-catalog-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: book-catalog
  template:
    metadata:
      labels:
        app: book-catalog
    spec:
      containers:
      - name: book-catalog
        image: $CONTAINER_REGISTRY/book_catalog:$VERSION
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://db_test_wb53_user:E0IJfLJaXOXR86Wx0X6ZCBmhsNLD8HQB@dpg-crn8mb08fa8c738bb4gg-a.oregon-postgres.render.com/db_test_wb53"
---
apiVersion: v1
kind: Service
metadata:
  name: book-catalog-service
spec:
  type: LoadBalancer
  selector:
    app: book-catalog
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000